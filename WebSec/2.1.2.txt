CREATE TABLE users ( id INT UNSIGNED PRIMARY KEY NOT NULL AUTO_INCREMENT, username VARCHAR(32) NOT NULL UNIQUE, salt VARCHAR(64) NOT NULL, passwordhash VARCHAR(64) NOT NULL);
CREATE TABLE history ( id INT UNSIGNED PRIMARY KEY NOT NULL AUTO_INCREMENT, user_id INT UNSIGNED NOT NULL, INDEX(user_id), query VARCHAR(2048) NOT NULL);

